export const parameters = {
    // Set layout prop here if you want all stories to be centered in the canvas (centered, padded and fullscreen)
    layout: 'centered',
    // Enable expanded mode globally to show property documentation alongside our controls
    controls: { expanded: true },
    backgrounds: {
      default: 'primary',
      values: [
        {
          name: 'primary',
          value: '#42b883',
        }
      ],
    }
};